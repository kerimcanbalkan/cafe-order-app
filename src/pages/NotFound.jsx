export default function NotFound({ className }) {
  <div className={`"container flex items-center justify-center ${className}`}>
    <h1 className="text-nord-0 text-2xl">404 NOT FOUND</h1>
  </div>;
}
